# Apps

This directory is reserved for the next major repository split.

Planned applications:

- `tauri-ui`
- `net-helper`

The current code still lives in `src/` and `src-tauri/` while the migration is being prepared.
